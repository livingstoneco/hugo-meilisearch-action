name: 'Deploy Search Index'
description: 'Deploy Hugo Search Index to MeiliSearch Instance'
inputs:
  key:  # id of input
    description: 'MeiliSearch Private API Key'
    required: true
    default: ''
  host:  # id of input
    description: 'MeiliSearch Host Addre4ss'
    required: true
    default: ''
runs:
  using: 'docker'
  image: 'Dockerfile'